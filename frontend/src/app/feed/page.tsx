import AppShell from "@/components/layout/AppShell";

export const metadata = {
  title: "FTS — Editorial Feed",
  description: "Prediction market news in editorial layout",
};

export default function FeedPage() {
  return <AppShell />;
}
